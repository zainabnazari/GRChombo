#Params for runtime inputs

verbosity = 0
chk_prefix = /scratch2/kclough/ASBH/M0.0a0.0f0.5chk
plot_prefix = /scratch2/kclough/ASBH/M0.0a0.0f0.5plt
#restart_file = /scratch2/kclough/ASBH/M0.0a0.0f0.5chk002500.3d.hdf5

initial_data_prefix = Oscilloton_data_iso/

# Set up grid spacings and regrid params
# NB - the N values need to be multiples of block_factor
N1 = 32
N2 = 32
N3 = 32

# Length of longest box side, dx_coarsest = L/N(max)
# NB if non cubic, this determines dx for all sides
L =  64

# Regridding
# Thresholds on the change across a cell which prompts regrid
regrid_threshold = 0.5
regrid_threshold_Pi = 0.002
regrid_threshold_chi = 0.4

# Level data
# Maximum number of times you can regrid above coarsest level
max_level = 5 # There are (max_level+1) grids, so min is zero
# Frequency of regridding at each level
# Need one for each level, ie max_level+1 items
# Generally you do not need to regrid frequently on every level
regrid_interval = 20 20 20 20 20 20 2 2 2 2
# Ratio of dx_coarse/dx_fine at each level, usually best to stick to 2
# Need one for each level, ie max_level+1 items
ref_ratio = 2 2 2 2 2 2 2 2 2 2
# Max box size
max_grid_size = 16
# Min box size
block_factor = 8
# Determines how exactly regridding tries to fit the tagging
tag_buffer_size = 3
fill_ratio = 0.6
#Periodicity and ghost cells
isPeriodic = 1 1 1
num_ghosts = 3

# Set up time steps
# dt will be dx*dt_multiplier on each grid level
# HDF5files are written every dt = L/N*dt_multiplier*checkpoint_interval
checkpoint_interval = 10
plot_interval = 1
dt_multiplier = 0.25
stop_time = 3
max_steps = 10000000

#Lapse evolution
lapse_power = 1.0
lapse_coeff = 2.0
lapse_advec_coeff = 1 # 1 makes the lapse gauge 1+log slicing

# Shift evolution coefficients
shift_advec_coeff = 0 # Usually no advection for beta
shift_Gamma_coeff = 0.75 # 
eta = 1.5 # This is beta_driver, usually of order 1/M_ADM of spacetime
SpatialBetaDriverRadius = 1e12

# CCZ4 parameters
# if using BSSN the kappa values should be zero
formulation = 0  # 1 for BSSN, 0 for CCZ4
kappa1 = 0.1
kappa2 = 0
kappa3 = 1.0
covariantZ4 = 1 # 0: default. 1: dampk1 -> dampk1/lapse

# coefficient for KO numerical dissipation
# NB must be less than 0.5 for stability
sigma = 0.5

# Change the gravitational constant of the Universe!
# Default is 1.0, for standard geometric units
# G_Newton = 1.0

# SF Initial data
scalar_mass = 1.0
f_axion = 0.5 #=0.92/sqrt(8*pi)

# SF parameter for centre - defaulted to center of grid
# so only uncomment if you want to place it off center
#oscilloton_center = 542 512 512

# Relaxation paramters
# how long and how fast to relax
relaxtime = 0.0
relaxspeed = 0.005
